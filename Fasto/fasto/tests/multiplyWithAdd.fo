fun int multiply(int x, int y) =
  if y == 0 || x == 0 
  then 0
  else 
    if y == 1 
    then x 
    else x + multiply(x, y - 1)


fun int main() =
  let x = read(int) in
  let y = read(int) in
  multiply(x,y)